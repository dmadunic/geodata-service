apply plugin: "war"

bootWar {
    mainClassName = "com.ag04.geodata.rest.GeoDataServiceApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
